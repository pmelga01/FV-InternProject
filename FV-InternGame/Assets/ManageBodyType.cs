using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ManageBodyType : MonoBehaviour
{
    public GameObject shirtSelection;
    public GameObject armsSelection;
    
    private SpriteRenderer shirt;
    private SpriteRenderer arms;
    // Start is called before the first frame update
    void Start()
    {
        //shirtSelection
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
